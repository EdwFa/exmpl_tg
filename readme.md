# 🧠 Проект: Работа с моделями в инференсе Together.ai

Интерактивный чат-ассистент на базе Streamlit для работы с мощными LLM через API [Together.ai](https://www.together.ai/). Поддерживает динамические системные промпты, профили специализаций (включая медицинские), управление параметрами генерации и ограничение контекста.

---

## 🌟 Особенности

- ✅ **Поддержка моделей Together.ai**  
  Включая `Qwen/Qwen3-Next-80B-A3B-Instruct`, `Llama-3`, `Mixtral`, `Gemma 2` и другие.
- 📁 **Динамические системные промпты**  
  Загрузка из YAML/JSON-файлов: меняйте поведение ассистента без перезапуска.
- 🎯 **Профили специализаций**  
  Преднастроенные шаблоны для онкологии ЖКТ, редких заболеваний (например, болезнь Фабри), обработки документов и др.
- 🔒 **Безопасность**  
  API-ключ не сохраняется — вводится в сессии или через Streamlit Secrets.
- 📏 **Управление контекстом**  
  Автоматическая обрезка истории чата под лимиты модели и заданную длину ответа.
- 🌍 **Русский язык**  
  Все ответы генерируются на русском (можно изменить в промпте).

---

## 📂 Структура проекта

```
project-root/
├── app.py                 # Основное Streamlit-приложение
├── prompts/               # Директория с профилями промптов
│   ├── default.yaml       # Базовый промпт
│   ├── oncology_gi.yaml   # Ассистент для онкологов ЖКТ
│   ├── fabry.yaml         # Эксперт по болезни Фабри
│   └── egrul.yaml         # Обработка юридических документов
├── .streamlit/
│   └── secrets.toml       # (опционально) Хранение API-ключа
└── README.md
```

---

## ⚙️ Требования

- Python ≥ 3.9
- Streamlit
- OpenAI SDK (совместимый с Together.ai)
- YAML/JSON-парсеры

Установка зависимостей:

```bash
pip install streamlit openai tiktoken pyyaml
```

---

## 🔑 Получение API-ключа

1. Зарегистрируйтесь на [Together.ai](https://www.together.ai/).
2. Перейдите в [API Keys](https://api.together.xyz/settings/api-keys).
3. Создайте новый ключ и сохраните его.

---

## ▶️ Запуск

### Вариант 1: Через переменную окружения или ввод в интерфейсе
```bash
streamlit run app.py
```
→ Введите API-ключ в боковой панели.

### Вариант 2: Через Streamlit Secrets (рекомендуется для локальной разработки)

Создайте файл `.streamlit/secrets.toml`:
```toml
TOGETHER_API_KEY = "ваш_ключ_здесь"
```

Затем запустите:
```bash
streamlit run app.py
```

---

## 📝 Создание собственных профилей

1. Добавьте новый файл в папку `prompts/`, например `my_domain.yaml`.
2. Укажите обязательное поле `system_prompt`:

```yaml
system_prompt: |
  Ты — эксперт в области [ваша область].
  Отвечай на русском языке, кратко и по делу.
  Не выдумывай факты.
domain: my_domain
```

3. Перезапустите приложение — профиль появится в списке выбора.

---

## 🛠 Параметры генерации

В боковой панели можно настроить:

- **Модель** — выбор из списка поддерживаемых.
- **Temperature** — креативность (0.0 — строго, 1.0 — случайно).
- **Max tokens** — максимальная длина ответа.
- **Top-p** — ядерная выборка.

---

## 📌 Использование в медицине

Проект изначально разработан для поддержки врачей-онкологов и исследователей редких заболеваний. Вы можете:

- Создавать профили под конкретные нозологии.
- Интегрировать с RAG-системой для запроса актуальных клинических руководств.
- Использовать для анализа выписок, отчётов, генетических данных.

> ⚠️ **Важно**: модель не заменяет врача. Используйте только в информационных целях.

---

## 🚀 Планы по расширению

- [ ] Интеграция с Qdrant для RAG
- [ ] Поддержка PDF/DOCX и автоматическое чанкирование
- [ ] Оценка релевантности найденных фрагментов
- [ ] Экспорт чата в Markdown/PDF

---

## 📬 Контакты и поддержка

Если вы работаете в области ИИ в медицине или разработки ИИ-ассистентов — свяжитесь с автором для совместной разработки!

---

> 📄 Лицензия: MIT (если не указано иное)  
> 🔄 Обновлено: январь 2026 г.